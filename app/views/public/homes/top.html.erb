<div class="container mt-5">
  <div class="row">
    <div class="col-sm-12 position-relative">
      <div class="row justify-content-center">
        <%= image_tag "top.png", class: "col-sm-10" %>
        <div class="text-light position-absolute" style="top: 47%; left: 42%;">
        <div class="mt-5 media_PC">
          <div class="mx-auto text-center">
            <% unless member_signed_in? %>
              <%= link_to '新規会員登録(無料)', new_member_registration_path, class: 'btn btn-outline-danger' %>
            <% end %>
          </div>
          <div class="mx-auto text-center">
            <% unless member_signed_in? %>
              <%= link_to 'ゲストログイン', members_guest_sign_in_path, method: :post, class: 'btn btn-outline-primary mt-3' %>
            <% end %>
          </div>
        </div>
      </div>
      </div>

        <div class="mb-3 media_SP">
          <div class="mx-auto text-center">
            <% unless member_signed_in? %>
              <%= link_to '新規会員登録(無料)', new_member_registration_path, class: 'btn btn-outline-danger btn-sm' %>
            <% end %>
          </div>
          <div class="mx-auto text-center">
            <% unless member_signed_in? %>
              <%= link_to 'ゲストログイン', members_guest_sign_in_path, method: :post, class: 'btn btn-outline-primary btn-sm mt-3' %>
            <% end %>
          </div>
        </div>
    </div>
  </div>
</div>
