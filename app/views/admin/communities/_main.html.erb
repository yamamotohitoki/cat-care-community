<div class="container mt-5">
  <div class="card">
    <div class="row">
      <div class="col-12">
        <% if community.image.present? %>
          <%= image_tag community.image, style: 'width: 100%; height: 200px;' %>
        <% else %>
          <%= image_tag 'blog-no-image.jpg', style: 'width: 100%; height: 200px;' %>
        <% end %>
      </div>
    </div>
    <div class="d-flex bd-highlight mt-3">
      <div class="p-2 bd-highlight"><h4><%= community.title %></h4><hr></div>
      <div class="mx-auto">
        <%= link_to 'コミュニティを削除', admin_community_path(community), method: :delete, class: "btn btn-sm btn-outline-danger" %>
      </div>
    </div>
    <div class="text-center">
      <p><%= community.body %></p>
    </div>
  </div>
</div>
<div class="container">

<table class="table">
  <thead>
    <tr>
      <th scope="col">参加メンバー</th>
    </tr>
  </thead>
  <tbody>
    <% community.members.each do |member| %>
      <tr>
        <td>
          <% if member.profile_image.attached? %>
            <%= image_tag member.profile_image, size: "50x50", class: "rounded-circle" %>
          <% else %>
            <%= image_tag 'member-no-image.jpg', size: "50x50", class: "rounded-circle" %>
          <% end %>
          <%= member.name %>さん
        </td>
        <td>
       <%= link_to '詳細', admin_member_path(member), class: 'btn btn-outline-primary btn-sm' %>
       </td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>


