<render>
<div class="container mt-3">
  <%= render 'admin/communities/main', community: @community %>
  <div class="card mt-5 mb-5" style="width: 18rem;">
    <div class="card-header">
      トピック一覧
    </div>
    <ul class="list-group list-group-flush">
      <% if @topics.any? %>
        <% @topics.each do |topic| %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <div>
              <%= image_tag topic.image, size: "50x50", class: "rounded-circle mr-3" %>
              <%= link_to topic.title, admin_community_topic_path(@community, topic) %>
            </div>
            <div>
             <i class="fa fa-commenting"></i>
              <%= topic.topic_comments.count %>
            </div>
          </li>
        <% end %>
      <% else %>
        <li class="list-group-item">
          トピックはまだありません
        </li>
      <% end %>
    </ul>
  </div>
</div>
